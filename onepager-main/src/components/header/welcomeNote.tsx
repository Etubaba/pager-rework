import HeaderTitle from './title';

const WelcomeNote = () => {
  return (
    <section className="mt-16">
      <HeaderTitle text="We are the best Recruitment," />
      <HeaderTitle text="Software development" />
      <HeaderTitle text="and IT Training company" />
    </section>
  );
};

export default WelcomeNote;
